import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontSize: 40,
    fontFamily: 'Cochin',
  },
  Button: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
